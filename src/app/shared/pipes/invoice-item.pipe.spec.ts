import { InvoiceItemPipe } from './invoice-item.pipe';

describe('InvoiceItemPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceItemPipe();
    expect(pipe).toBeTruthy();
  });
});
