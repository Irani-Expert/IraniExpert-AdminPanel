import { JalaliTimePipe } from './jalali-time.pipe';

describe('JalaliTimePipe', () => {
  it('create an instance', () => {
    const pipe = new JalaliTimePipe();
    expect(pipe).toBeTruthy();
  });
});
