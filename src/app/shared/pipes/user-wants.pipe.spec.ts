import { UserWantsPipe } from './user-wants.pipe';

describe('UserWantsPipe', () => {
  it('create an instance', () => {
    const pipe = new UserWantsPipe();
    expect(pipe).toBeTruthy();
  });
});
